Logins:
Die Authentifizierungen rund um das System werden unter Ubuntu in der Datei auth.log festgehalten. 
Um z.B. die letzten 30 Zeilen dieser Log-Datei zu sehen, geben die den Befehl ein:
	tail -n 30 /var/log/auth.log